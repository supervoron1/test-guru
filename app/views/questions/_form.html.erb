<%= form_with(model: [@test, @question], local: true) do |form| %>
  <div class="">
    <%= form.label :body %>
    <%= form.text_area :body, class: "" %>
  </div>

  <div class="">
    <%= form.submit class: "" %>
    <%= link_to 'Back', test_path(@test), class: "" %>
  </div>
<% end %>
</br>
